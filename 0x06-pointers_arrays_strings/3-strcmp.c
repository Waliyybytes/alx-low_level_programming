#include <stdio.h>
#include <string.h>
#include <main.h>
/**
 * _strcmp - function to comapre two strings
 *
 * @s1: destination string
 * @s2: source string
 * Return: returns an integer positive , 0  or negative
 * if greater, match or less
 */

char _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
