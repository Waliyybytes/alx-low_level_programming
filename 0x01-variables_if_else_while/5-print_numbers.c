#include <stdio.h>
/**
 * main - Entry point , prints numbers
 *
 * Return: Always zero(success)
 */

int main(void)
{

	int i = 0;

	while (i < 123)
	{
		putchar(i);
		i++;
	}
	putchar(10);

	return (0);

}
