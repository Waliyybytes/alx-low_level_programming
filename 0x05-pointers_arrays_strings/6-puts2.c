#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * puts2 - prints every other character
 *
 * @str: string to print
 * Return: nothing
 */

void puts2(char *str)
{
	int i;

	for (i = 0; i < strlen(s); i += 2)
		_putchar(s[i]);

	_putchar('\n');
}

