#include <stdio.h>
#include "main.h"
#include <string.h>
#include <stdlib.h>

/**
 * _atoi - prints to stdout with a new line
 *
 * @s: string to convert to int
 * Return: integer
 */

int _atoi(char *s)
{
	return (atoi(s));
}

